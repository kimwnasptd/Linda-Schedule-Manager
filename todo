audio_handler:	Create the interface and the code for
				the speech-to-text part.

Agent:			* Add more Intents { Remove Event, Info about Event }
				Implement a conversation system (Keep asking questions until all needed parameters are fulfilled
				Responses
				* Cancel Intent, 
				* Add more words to categorize entities

schedule_handler:
				d
